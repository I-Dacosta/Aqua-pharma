import puppeteer from 'puppeteer';

(async () => {
    // Just output nothing if we can't do it easily, we'll just write a regular GSAP effect.
    console.log("We will use GSAP to implement the stack over effect.");
})();
